{
  "name": "Albedo",
  "version": "1.0.0",
  "main": "build/main.js",
  "repository": "git@github.com:reealy/Albedo.git",
  "author": "reealy_ <van.pham2809@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "yarn run build && node build/main.js",
    "test": "echo \"Error: no test specified\"",
    "dev": "tsc-watch --onSuccess \"node ./build/main.js\"",
    "clean": "rimraf build && rimraf tsconfig.tsbuildinfo && rimraf yarn-error.log"
  },
  "dependencies": {
    "discord.js": "^13.8.0",
    "dotenv": "^16.0.1",
    "ts-node": "^10.8.1",
    "typescript": "^4.7.3"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "tsc-watch": "^5.0.3"
  }
}
